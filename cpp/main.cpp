#include <iostream>
#include <string.h>
#include <vector>
#include <map>

#define forsn(i, s, n) for(int i = (int) s; i < (int) n; i++)
#define forn(i, n) forsn(i, 0, n)

using namespace std;

#include "knowledge/knowledge.cpp"

void testing() {
  /*
  Knowledge math;
  Node node;
  node.symbol = math.alphabet["F_0"];
  vector<Node> list1;
  list1.push_back(node);
  vector<Node> list2;
  cout << node.belongsToVector(list1) << endl;
  cout << node.belongsToVector(list2) << endl;
  */
}

int main() {
  testing();
  return 0;
}

/*

Comments:
  - ChildrenFormat is not being used
  - Make function add to wwk
  - Google set for C++

*/
